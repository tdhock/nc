language: r

sudo: required

apt_packages:
  - texlive
  - ghostscript
  - texlive-fonts-extra
  - texinfo  

notifications:
  email:
    on_success: change
    on_failure: change

r_github_packages:
  - jimhester/covr

after_success:
  - Rscript -e "covr::coveralls()"
